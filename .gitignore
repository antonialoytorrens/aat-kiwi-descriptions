# kiwi-ng build output, including the generated description-root symlink
# farm and merged BSP overlay that scripts/apply_bsp.py writes into it
# (see the $(PLATFORMS) recipe in Makefile).
/build/

__pycache__/

# Board support package clone, fetched by scripts/fetch_bsp.py (make bsp-pull).
/bsp/

# Generated config.sh
config.sh
