<image>
    <!-- Stable (Bookworm) for x86_64, i386, RPi and TF101 -->
    <!--
    <repository type="apt-deb" alias="debian-bookworm" profiles="common_debian">
        <source path="http://deb.debian.org/debian/dists/bookworm"/>
    </repository>
    <repository type="apt-deb" alias="debian-security-bookworm" profiles="common_debian">
        <source path="http://security.debian.org/debian-security/dists/bookworm"/>
    </repository>
    -->
    <repository type="apt-deb" distribution="bookworm" components="main contrib non-free" repository_gpgcheck="false">
        <source path="http://deb.debian.org/debian"/>
    </repository>
    <repository type="apt-deb" distribution="bookworm" components="main contrib non-free" repository_gpgcheck="false">
        <source path="http://security.debian.org/debian-security"/>
    </repository>

    <!-- Unstable (Sid) for Odroid (Example of version differentiation) -->
    <repository type="apt-deb" distribution="sid" components="main" repository_gpgcheck="false">
        <source path="http://deb.debian.org/debian"/>
    </repository>
    <!-- Sid generally doesn't use security repo directly in the same way, but keeping structure similar if needed 
         or just omit security for sid as it moves too fast. -->
</image>
